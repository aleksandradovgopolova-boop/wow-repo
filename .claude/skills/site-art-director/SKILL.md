---
name: site-art-director
description: >-
  Define the visual direction for an entire generated site — typography, colour,
  composition, imagery, density, motion, and visual prohibitions — derived from
  the product's meaning. Runs rarely (per site, not per content change).
---

# site-art-director

This skill is the **Claude Code binding** of WowRepo pipeline stage 3. The
pipeline is provider-neutral, so the full specification lives once, outside this
tool, and every binding defers to it:

> **Follow [`pipeline/stages/03-site-art-director.md`](../../../pipeline/stages/03-site-art-director.md) exactly.**

That document is the single source of truth for what you decide (the
art-direction doc + token *values*), the hard rules (derive from meaning, change
values not structure, state prohibitions), the process, and the definition of
done. Do not restate or diverge from it here.

See [`pipeline/README.md`](../../../pipeline/README.md) for how the stages fit
together and how the same pipeline runs on other models (Kimi, OpenAI, …).

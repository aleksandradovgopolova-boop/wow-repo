"""intelligence — продуктовая аналитика: здоровье продукта, эффект, триггеры эволюции (3 модуля).

Кольцо Intelligence из AGENTS.md: зависит от Kernel и ЧИТАЕТ его события, но Kernel от него НЕ
зависит — изменение продукта не должно требовать заполнения продуктовой онтологии. Пока эти модули
лежали в `lifecycle` вместе с состоянием работы (lifecycle_store, workitem, run_report), инвариант
был невыразим в терминах пакетов и честно значился непроверяемым. Теперь он проверяется слоями:
`intelligence` стоит ВЫШЕ ядра, а зависимость вверх запрещена (packages/layering.yaml).

Реальный код — здесь; плоское имя (`tools/<module>.py`) осталось алиасом через `sys.modules`.
"""

"""devtools — инструменты разработки САМОГО кита; в child-репозиторий не едут.

Состав — не выдумка, а зеркало installer.DEV_ONLY_TOOLS: единственного объявления о том, что
исключено из поставки. Держать их в продуктовых пакетах значило бы, что пакет содержит модули,
которые пользователь никогда не получит.
"""

"""Валидаторы кита: проверки инвариантов, запускаемые CI, `ai-ops doctor` и pytest.

Пакет, а не плоский каталог, с v3.34: `validation` в site-packages — родовое имя, и в окружении
с установленным китом чужой `import validation` резолвился сюда. Модули остались запускаемыми
скриптами (`python3 <путь>/validate_X.py`) — это их основной способ вызова, и пакетность его не
отменяет: `_bootstrap` ставит пути по маркеру VERSION, а не по глубине файла.
"""

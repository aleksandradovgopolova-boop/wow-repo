"""planning — контур Planning & Execution модели продуктового репозитория (v3.35.0).

Пакет отвечает на четыре вопроса, которые репозиторий обязан уметь отвечать в любой момент:
где мы сейчас, что идёт прямо сейчас, что заблокировано и чем, какую работу имеет смысл взять
следующей. Модель контуров — `registry/product-operating-model.yaml`.

Слой `capabilities` (`packages/layering.yaml`): пакет читает `lifecycle` (статус WorkItem'а и
реестр активных работ) и `shared`. Никто из ядра его не импортирует — точка входа одна, CLI.
"""

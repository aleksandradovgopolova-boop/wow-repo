"""ai_ops_kit.checks — ЧИСТАЯ проверяющая логика валидаторов, вынесенная вниз (v3.38, лента №4).

ПОЧЕМУ ОТДЕЛЬНЫЙ ПАКЕТ. Рантайм (engine/lifecycle/providers/security/intelligence) звал валидаторы
как БИБЛИОТЕКУ ради их функций `check(...)`, а валидаторы живут в `validation` (слой entrypoints, НАД
ядром). Получались восходящие рёбра — замороженный список `known_violations` в `packages/layering.yaml`,
который тянул граф в цикл. `layering.yaml` предлагал развязку через подпроцесс; выбран более чистый
путь — вынести САМУ ЛОГИКУ вниз: пакет `checks` в слое primitives держит чистые функции (только
stdlib), и рантайм импортирует их ВНИЗ (разрешено), а CLI-валидаторы в `validation` — тоже вниз.
Восходящих рёбер не остаётся, подпроцессов не нужно.

Контракт пакета (v3.38, лента №5 расширила): модули `checks` зависят ТОЛЬКО от stdlib и pyyaml и
НЕ импортируют ничего из ai_ops_kit выше foundation — это и есть инвариант, который держит слой
(проверяет `validate_layering`). Форма функций бывает двух видов:

  • ЧИСТЫЕ `check(data) -> list[str]` (список ошибок, пустой = валидно) — большинство: политика
    памяти, форма артефактов и вердиктов. Ввода-вывода нет, файл читает CLI-обёртка.
  • READ-ONLY проверки каталога артефакта — `validate_dir(feature_dir)`, `check_feature(dir)`,
    `check_registry(adr_dir)`: их логика неразделима с чтением файлов (существование артефактов,
    парсинг таблиц, обход реестра), разложить на «чистое check(data) + I/O у вызывателя» нельзя,
    не переписав обход. Они вынесены сюда ЦЕЛИКОМ, потому что смысл переноса — дать рантайму звать
    проверку ВНИЗ (в свой слой), а не тянуть `validation` (entrypoints) вверх; read-only чтение
    диска этому не мешает, а вот запись (render, генерация) осталась выше — в ядре и в CLI.

Обёртки CLI (argparse, коды возврата, демо-данные селфтестов) остаются в `ai_ops_kit/validation/*`
и ре-экспортируют логику отсюда для обратной совместимости.
"""

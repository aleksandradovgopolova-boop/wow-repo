"""ai_ops_kit — пакетная поверхность кита (шим, v3.30).

Дерево пока плоское: модули лежат в tools/. Этот пакет даёт им осмысленные имена и границы, НЕ
перемещая файлы. Каждый подмодуль — АЛИАС через sys.modules, то есть `ai_ops_kit.engine.tool_broker`
и `tool_broker` — ОДИН И ТОТ ЖЕ объект модуля.

Так сделано намеренно: обычный шим через `from X import *` создал бы второй объект модуля со
своим состоянием, и любое пересоздание глобали (как drain_call_stats в orchestrator_usage)
разъехалось бы между двумя копиями. Этот класс дефектов уже стоил отдельной отладки.

Порядок переезда: сначала имена (здесь), потом вызывающие переходят на них, и только потом файлы
физически переносятся, а алиасы разворачиваются в обратную сторону.
"""

# .ai/custom

Оверлей: ваши правки поверх managed-слоя. Кит сюда не пишет и это не перезаписывает.

Файл создан AI Ops, чтобы каталог пережил клон: git не хранит пустые
каталоги, и без него установка после `git clone` выглядит неполной.
